const userSalaries = {
    Ella: '30000 som',
    Sophia: '40000 som',
    Ellie: '50000 som'
}
alert(userSalaries.Ella);
alert(userSalaries.Sophia);

const plus = {
    a: 5,
    b: 6,
    c: 10
}

alert(plus.a+plus.b+plus.c);

let data = 5;
let number;
number = data ? 3 : 5;
console.log(number);